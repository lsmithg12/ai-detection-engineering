# gaps/data-sources/sysmon_eid_22.yml
# Data source gap tracking — Phase 3
gap_id: GAP-004
source_type: sysmon
event_id: 22
event_name: DNSQuery
status: partially_available  # gap | partially_available | onboarded
priority: high  # critical | high | medium | low
affected_techniques:
  - T1071.001
  - T1071.004
  - T1568
simulator_support: true
cribl_pipeline: cim_normalize  # Phase 3: regex parser deployed via cribl_pipeline.py
ecs_fields_expected:
  - dns.question.name
  - dns.question.type
  - process.name
  - event.code
resolution_notes: |
  Added to the simulator in PR #17. Needed for detecting Fawkes domain fronting
  (T1071.001), DNS C2 (T1071.004), and dynamic resolution (T1568). Cribl regex
  parser for EID 22 now exists in cim_normalize pipeline (cribl_pipeline.py) —
  extracts QueryName and Image fields. Remaining work: validate ECS field mapping
  end-to-end and move status to onboarded once confirmed in production.
created_date: "2026-03-14"
updated_date: "2026-03-15"
